/*
 * Link:
 *  https://codeforces.com/problemset/problem/2168/C
 */

#include <bits/stdc++.h>
using namespace std;

void first() {
    int t; // num of testcases
    cin >> t;

    for (int i=0; i<t; i++) {
        int x;
        cin >> x;
    }
}

void second() {

}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    string run;
    cin >> run;
    cout << run;

    if (run.compare("first") == 0)
        first();
    else
        second();

    return 0;
}
